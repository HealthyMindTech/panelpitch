{
  "name": "experts-mind",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -noEmit && esbuild src/app.ts --bundle --platform=node --sourcemap=inline --minify --outdir=build",
    "esbuild": "esbuild"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.106",
    "@types/node": "^18.7.21",
    "@types/node-fetch": "^2.6.2",
    "@types/uuid": "^9.0.0",
    "esbuild": "^0.17.5",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "aws-sdk": "^2.1223.0",
    "openai": "^3.2.1"
  }
}
